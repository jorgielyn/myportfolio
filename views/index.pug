extends layout.pug

block content
  article.grid-container.portfolio-intro
    div


      h2 Hi I'm Jorgielyn and here's what I can do.


      p.lead.text-light
        | This website is part of my Project. It seamlessly uses Express and Pug to showcase my other projects I've completed in the program.
  article.grid-container.portfolio-index

    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3


      each project in projects

        .cell
          a(href=`/projects/${project.id}`)
            img.thumbnail(src=`/images/${project.image_urls[0]}`)
            h5 #{project.project_name}
